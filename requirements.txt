# Archivo: /requirements.txt

# --- Core de Flask ---
flask
python-dotenv         # Para leer el archivo .env con configuraciones

# --- Base de Datos (Tu elección: PostgreSQL) ---
flask-sqlalchemy      # ORM para manejar la BBDD
flask-migrate         # Para gestionar cambios en el modelo de datos
psycopg2-binary       # El "driver" que conecta Python con PostgreSQL

# --- Autenticación y Seguridad (Tu requisito de 'bcrypt') ---
flask-login           # Maneja las sesiones de usuario (login/logout)
flask-bcrypt          # Para encriptar las contraseñas 

# --- Motor de Análisis (Tu elección) ---
pandas                # [cite: 108]
scikit-learn          # [cite: 108]
joblib                # Para guardar y cargar modelos entrenados

# --- Reportes (Tu requisito RF-07) ---
WeasyPrint            # Para exportar vistas HTML a PDF

# --- Servidor de Producción ---
gunicorn              # Servidor WSGI para producción (NO usar en desarrollo)